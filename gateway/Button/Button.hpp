/**
 * @file Button.hpp
 * @author Grzegorz Krajewski
 *
 * Button class.
 *
 * @license GPL V2
 */

// #include <OneButton.h>

// class Button : public OneButton {

//   const uint8_t pin;
//   const bool activelow;
// //   const bool pullup;

//   Button(const uint8_t& _pin, const bool _activelow = true) :
//     pin(pin), activelow(_activelow) {}

//   Button(void (*click), const uint8_t& _pin, const bool _activelow = true) :
//     pin(pin), activelow(_activelow) {
//       attachClick(&click);
//     }
// };